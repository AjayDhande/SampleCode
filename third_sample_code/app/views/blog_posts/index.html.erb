<%= render :partial => "heading" %>

<% if @user.self? %>
  <div class="actions">
    <%= button_to_new_blog_post %>
  </div>
<% end %>

<%= paginate @blog_posts %>

<div class="blog_posts">
  <% @blog_posts.each do |blog_post| %>
    <%= render blog_post %>
  <% end %>
</div>

<%= paginate @blog_posts %>
